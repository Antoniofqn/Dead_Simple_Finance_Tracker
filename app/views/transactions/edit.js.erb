document.querySelector("#myEditModal").insertAdjacentHTML(
  "afterend",
  "<%= escape_javascript(render partial: 'edit', collection: @transactions, as: :transaction) %>"
);
document.querySelector("#myEditModal").modal('show');
